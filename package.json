{
  "name": "causerie",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first MVP for Causerie, an AI French speaking partner.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "build:public": "node scripts/build-static.mjs --public",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
